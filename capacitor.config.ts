import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Projeto-Ajuda-Me-Mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
